<h1>Listing materia_primas</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Código</th>
      <th>Descrição</th>
      <th>Quantidade</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @materia_primas.each do |materia_prima| %>
      <tr>
        <td><%= materia_prima.nome %></td>
        <td><%= materia_prima.codigo %></td>
        <td><%= materia_prima.descricao %></td>
        <td><%= materia_prima.quantidade %></td>
        <td><%= link_to 'Exibir', materia_prima %></td>
        <td><%= link_to 'Editar', edit_materia_prima_path(materia_prima) %></td>
        <td><%= link_to 'Apagar', materia_prima, method: :delete, data: { confirm: 'Tem certeza disso?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Matéria-prima', new_materia_prima_path %>
